// The app router served by Next.js lives at the repo root, so this route
// re-exports the handler defined in dashboard/app/api/commands/route.ts.
export { GET } from "../../../dashboard/app/api/commands/route";
